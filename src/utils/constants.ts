const runningOn = "running on ";
const unknownError = "Error desconocido";
const apiIsRunning = "API Server running";

export const SERVER_MESSAGES = {
  runningOn,
  unknownError,
  apiIsRunning,
};
