# Server Configuration
PORT=3000
HOST=localhost
NODE_ENV=development

# Logging
MORGAN_FORMAT=dev

# Database
DB_PATH=./src/database/tasks.db

# CORS - Origins permitidos (separados por coma para produccion)
# ALLOWED_ORIGINS=https://tudominio.com,https://app.tudominio.com
